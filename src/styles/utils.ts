const glowColor = "rgba(59, 130, 246, 0.35)"; // Tailwind's blue-500 with reduced opacity

export const shadow = `0 0 5px ${glowColor}, 0 0 10px ${glowColor}, 0 0 15px ${glowColor}, 0 0 20px ${glowColor}`; // Custom box shadow using the glow color
